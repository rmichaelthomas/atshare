# Server port (LiteSpeed reverse proxy should forward to this)
PORT=3001

# Public URL of the server (used for OAuth redirect URIs)
PUBLIC_URL=https://atshare.social

# Cookie secret for signing session cookies
COOKIE_SECRET=change-me-to-a-random-string

# Path to SQLite database file
DB_PATH=./data/atshare.db
